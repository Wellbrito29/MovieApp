import React from 'react';
import {View, Text} from 'react-native';

const Favorites = () => {
  return (
    <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
      <Text style={{fontFamily: 'Roboto-Bold', fontSize: 20}}>Hom!!e</Text>
    </View>
  );
};

export default Favorites;
